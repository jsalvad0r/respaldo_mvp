// Placeholder: Vercel's configured build command runs this before `next build`.
// v0.app normally generates this file when deploying from its own UI; when
// pushing via plain git, it doesn't exist, which fails the build. This no-op
// stub keeps the existing Vercel build command working either way.
